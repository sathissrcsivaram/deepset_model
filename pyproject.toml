[project]
name = "baseline-deepsets"
version = "0.1.0"
description = "Standalone baseline DeepSets heatmap model and event generator"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.3",
    "pandas>=3.0.1",
    "torch>=2.10.0",
]

